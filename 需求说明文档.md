# 图书管理系统 — 软件需求说明文档（SRS）

**文档版本**：1.0  
**对应工程**：Books-Management-System（Spring + Spring MVC + MySQL）  
**说明**：本文档描述**当前已实现**的系统行为与数据约束，作为黑盒测试的需求依据；文中注明与项目 README 或设想不一致之处。

---

## 1. 引言

### 1.1 编写目的

本文档用于：

- 明确系统目标用户、功能边界与核心业务规则；
- 支撑课程作业中的黑盒测试用例设计（等价类、边界值、决策表等）；
- 作为测试计划、测试报告中「需求追溯」的基准。

### 1.2 范围

本系统为 **B/S 架构** 的 Web 应用，部署后通过浏览器访问。主要覆盖：

- **管理员**：图书与读者信息的维护、图书借还登记、全馆借阅流水查看、管理员密码修改；
- **读者**：登录后图书检索与详情查看、个人借阅记录查看、读者密码与个人信息维护。

**不包含**（当前代码库未实现）：独立移动端 App、在线支付、邮件/短信通知、完整的工作流审批。

### 1.3 术语与缩写

| 术语 | 含义 |
|------|------|
| 图书状态 `state` | 在 `book_info` 中标识图书是否可借；业务中与借出逻辑联动（借出后置为不可借，归还后恢复）。 |
| 读者证 `reader_card` | 存储读者登录账号（与 `reader_id` 对应）、姓名、密码及 `card_state`。 |
| 挂失 / 超期 | 数据库或界面存在字段或占位，但**部分逻辑未在控制器层完整实现**，见第 6 节。 |

---

## 2. 运行与技术约束

| 项目 | 说明 |
|------|------|
| 构建与运行 | Maven；可使用 Jetty 插件（默认端口以项目配置为准，常见为 `http://localhost:9000`）。 |
| 服务端 | Java 8、Spring / Spring MVC、JDBC（`JdbcTemplate`）。 |
| 数据库 | MySQL；初始化脚本见仓库根目录 `library.sql`。 |
| 客户端 | 现代浏览器；前端使用 JSP + Bootstrap，部分页面依赖静态资源（jQuery 等）。 |
| 字符集 | UTF-8。 |

---

## 3. 用户角色与权限

| 角色 | 说明 | 会话标识 |
|------|------|----------|
| 管理员 | 使用 `admin` 表账号登录 | Session 属性 `admin` |
| 读者 | 使用 `reader_card` 账号登录 | Session 属性 `readercard` |

**鉴权方式说明**：登录成功后在 Session 中写入上述对象；各功能通过页面入口与菜单组织，**并非所有 URL 均在服务端逐条做角色级拦截**（测试时需结合 Session 状态设计用例）。

---

## 4. 功能需求

### 4.1 登录与注销

| 需求编号 | 功能概述 | 主要入口 / 接口 | 预期行为摘要 |
|----------|----------|-------------------|--------------|
| FR-LOGIN-01 | 进入登录页 | `GET /`、`GET /login.html` | 使 Session 失效后展示登录页。 |
| FR-LOGIN-02 | 登录校验 | `POST /api/loginCheck`（参数：`id`、`passwd`） | 校验管理员或读者账号密码；成功则返回 JSON `stateCode`（管理员为 `"1"`，读者为 `"2"`），并在 Session 中写入 `admin` 或 `readercard`；失败返回 `stateCode` 为 `"0"`。 |
| FR-LOGIN-03 | 注销 | `GET /logout.html` | 清除 Session，重定向至登录页。 |
| FR-LOGIN-04 | 进入管理员主页 | `GET /admin_main.html` | 管理员工作台页面。 |
| FR-LOGIN-05 | 进入读者主页 | `GET /reader_main.html` | 读者工作台页面。 |

### 4.2 管理员 — 密码修改

| 需求编号 | 功能概述 | 主要入口 | 预期行为摘要 |
|----------|----------|----------|--------------|
| FR-ADM-PWD-01 | 修改管理员密码页 | `GET /admin_repasswd.html` | 展示修改密码表单。 |
| FR-ADM-PWD-02 | 提交修改 | `POST/GET`（项目映射为）`/admin_repasswd_do`（参数：`oldPasswd`、`newPasswd`、`reNewPasswd`） | 校验旧密码；成功则更新密码并提示成功，否则提示旧密码错误或修改失败。 |

### 4.3 图书管理（管理员）

| 需求编号 | 功能概述 | 主要入口 | 预期行为摘要 |
|----------|----------|----------|--------------|
| FR-BOOK-01 | 图书列表 | `GET /allbooks.html` | 展示全部图书列表。 |
| FR-BOOK-02 | 按关键词查询 | `GET /querybook.html`（参数：`searchWord`） | 匹配则展示结果列表；无匹配则给出「没有匹配的图书」类提示。 |
| FR-BOOK-03 | 图书详情（管理员） | `GET /bookdetail.html`（参数：`bookId`） | 展示指定图书详细信息。 |
| FR-BOOK-04 | 新增图书页 | `GET /book_add.html` | 展示新增表单。 |
| FR-BOOK-05 | 提交新增 | `GET/POST`（映射）`/book_add_do.html`，表单绑定 `BookAddCommand` | 写入 `book_info`；成功或失败后有 Flash 提示并重定向至图书列表。 |
| FR-BOOK-06 | 编辑图书页 | `GET /updatebook.html`（参数：`bookId`） | 加载图书进入编辑页。 |
| FR-BOOK-07 | 提交编辑 | `/book_edit_do.html`（参数含 `id` 及图书字段） | 更新图书信息；成功/失败 Flash 提示。 |
| FR-BOOK-08 | 删除图书 | `GET /deletebook.html`（参数：`bookId`） | 删除指定图书；成功或失败提示。 |

### 4.4 图书检索与详情（读者）

| 需求编号 | 功能概述 | 主要入口 | 预期行为摘要 |
|----------|----------|----------|--------------|
| FR-R-BOOK-01 | 检索页 | `GET /reader_querybook.html` | 读者图书检索界面；可展示 Flash 中的查询结果或错误信息。 |
| FR-R-BOOK-02 | 执行检索 | `/reader_querybook_do.html`（参数：`searchWord`） | 有匹配则 Flash 图书列表并重定向回检索页；无匹配则 Flash 错误信息。 |
| FR-R-BOOK-03 | 图书详情（读者） | `GET /readerbookdetail.html`（参数：`bookId`） | 展示图书详细信息。 |

### 4.5 读者信息管理（管理员）

| 需求编号 | 功能概述 | 主要入口 | 预期行为摘要 |
|----------|----------|----------|--------------|
| FR-READ-01 | 读者列表 | `GET /allreaders.html` | 展示读者信息列表。 |
| FR-READ-02 | 删除读者 | `GET /reader_delete.html`（参数：`readerId`） | 删除读者相关记录；成功/失败提示。 |
| FR-READ-03 | 编辑读者（管理员） | `GET /reader_edit.html`（参数：`readerId`） | 进入编辑页。 |
| FR-READ-04 | 保存读者编辑（管理员） | `/reader_edit_do.html` | 更新读者信息与关联读者证姓名等；规则中含姓名变更时的联动更新。 |
| FR-READ-05 | 新增读者页 | `GET /reader_add.html` | 新增读者表单。 |
| FR-READ-06 | 提交新增读者 | `/reader_add_do.html` | 新增 `reader_info` 与关联 `reader_card`。 |

### 4.6 读者自助（登录后）

| 需求编号 | 功能概述 | 主要入口 | 预期行为摘要 |
|----------|----------|----------|--------------|
| FR-R-SELF-01 | 查看个人信息 | `GET /reader_info.html` | 根据当前 Session 中读者证获取 `reader_id` 并展示 `reader_info`。 |
| FR-R-SELF-02 | 编辑个人信息页 | `GET /reader_info_edit.html` | 编辑页。 |
| FR-R-SELF-03 | 保存个人信息 | `/reader_edit_do_r.html` | 更新读者基本信息及必要时读者证姓名。 |
| FR-R-SELF-04 | 修改密码页 | `GET /reader_repasswd.html` | 读者修改密码表单。 |
| FR-R-SELF-05 | 提交读者密码修改 | `/reader_repasswd_do.html` | 校验旧密码通过后更新 `reader_card.passwd`。 |

### 4.7 借阅业务

| 需求编号 | 功能概述 | 主要入口 | 预期行为摘要 |
|----------|----------|----------|--------------|
| FR-LEND-01 | 办理借书页（管理员） | `GET /lendbook.html`（参数：`bookId`） | 展示指定图书借出表单。 |
| FR-LEND-02 | 确认借出 | `/lendbookdo.html`（图书 `id`、`readerId`） | 调用借阅服务：在 `lend_list` 增加记录，并将图书状态更新为借出态；界面提示借阅结果（注意：当前控制器在失败分支仍可能提示成功，测试时应记录**实际数据库状态**）。 |
| FR-LEND-03 | 办理还书 | `GET /returnbook.html`（参数：`bookId`） | 归还图书：更新借阅记录归还日期，恢复图书可借状态；成功/失败提示。 |
| FR-LEND-04 | 全馆借阅流水 | `GET /lendlist.html` | 管理员查看借阅列表。 |
| FR-LEND-05 | 我的借阅 | `GET /mylend.html` | 当前登录读者查看本人借阅记录；列表展示借出日期、归还日期及是否已还（页面逻辑基于 `back_date` 是否为空）。 |

**核心业务流程（建议作为端到端测试主线）**

1. 读者：`/api/loginCheck` 登录 → `/reader_querybook.html` 检索 → `/readerbookdetail.html` 查看图书 →（借书由管理员在后台完成）→ `/mylend.html` 查看借阅。  
2. 管理员：登录 → `/allbooks.html` 维护图书 → `/lendbook.html` / `/lendbookdo.html` 借出 → `/returnbook.html` 归还 → `/lendlist.html` 查询流水。

---

## 5. 数据需求（概要）

数据存储于 MySQL，核心表与用途如下（字段细节以 `library.sql` 为准）。

| 表名 | 用途 |
|------|------|
| `admin` | 管理员账号与密码。 |
| `book_info` | 图书元数据及在架状态 `state`。 |
| `class_info` | 图书分类（中图法大类）。 |
| `reader_info` | 读者档案（姓名、性别、出生日期、地址、电话等）。 |
| `reader_card` | 读者登录凭证：`reader_id`、`name`、`passwd`、`card_state`。 |
| `lend_list` | 借阅流水：`book_id`、`reader_id`、`lend_date`、`back_date`。 |

**业务规则（来自实现归纳）**

- 借书：图书需处于可借状态；成功后写入借阅记录并更新图书 `state`。  
- 还书：根据未归还记录更新 `back_date`，并恢复图书 `state`。  
- 读者证 `card_state` 在数据层存在；**登录接口未依据该字段拒绝挂失卡登录**（若作业测试「挂失」，需先与本文档或缺陷说明一致）。

---

## 6. 已知限制与文档/ README 差异

以下内容便于测试范围界定，避免需求歧义：

| 项目 | 说明 |
|------|------|
| 读者证挂失 | `reader_card.card_state` 字段存在，但 **登录流程未校验挂失状态**，系统未提供单独「挂失/解挂」管理界面。 |
| 超期提醒 | README 提及超期提醒；页面存在「超期」展示占位，但 **条件未完整实现**，当前以「未还 / 已还」为主进行展示判断。 |
| 借书失败提示 | `LendController.bookLendDo` 中失败分支文案可能与实际业务不一致，测试应以数据库与列表页状态为准。 |

---

## 7. 非功能需求（概要）

| 类型 | 要求 |
|------|------|
| 易用性 | 管理员与读者菜单分离；操作结果使用 Flash 消息提示。 |
| 可维护性 | 分层结构：Controller / Service / Dao；配置见 `src/main/resources/book-context.xml` 与 `web.xml`、`*-servlet.xml`。 |
| 安全性 | 密码明文存储于数据库（课程项目现状）；生产环境不适用——测试文档可记为已知风险。 |

---

## 8. 文档追溯

| 工件 | 路径或说明 |
|------|------------|
| 数据库脚本 | `library.sql` |
| Web 控制器 | `src/main/java/com/book/web/*.java` |
| 页面 | `src/main/webapp/WEB-INF/jsp/` |

---

**修订记录**

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | 2026-04-21 | 初稿，依据当前源码与库表整理。 |
