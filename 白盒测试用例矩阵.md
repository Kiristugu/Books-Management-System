# 图书管理系统白盒测试用例矩阵

本文档用于支撑 `HW1` 中白盒测试分工，覆盖代码覆盖、基本路径覆盖、数据流分析三类要求。  
测试对象以 `Controller -> Service -> Dao` 核心链路为主，优先保障登录、借还书、图书维护、读者自助四条主线。

## 1. 覆盖目标

- 目标覆盖率：语句覆盖率与分支覆盖率达到并尽量超过 95%
- 关键方法路径覆盖：`loginCheck`、`bookLendDo`、`bookReturn`、`addBookDo`、`bookEditDo`、`readerRePasswdDo`、`reader_edit_do_r`
- 数据流覆盖：对 `bookId`、`readerId`、`readerCard`、`birth`、`searchWord` 等关键变量进行定义-使用链验证

## 2. 测试矩阵（按模块）

说明：
- 用例编号格式：`WB-模块-序号`
- 覆盖类型：`语句` / `分支` / `基本路径` / `数据流`
- 优先级：`P0`（必须）/ `P1`（高）/ `P2`（补充）

### A. 登录与会话（`LoginController` + `LoginService`）

- `WB-LOGIN-01`
  - 目标方法：`/api/loginCheck`
  - 覆盖类型：分支 + 基本路径
  - 输入：管理员合法账号密码
  - 预期：返回 `stateCode=1`；Session 写入 `admin`
  - 断言点：管理员分支命中；失败分支不命中
  - 优先级：P0

- `WB-LOGIN-02`
  - 目标方法：`/api/loginCheck`
  - 覆盖类型：分支 + 基本路径
  - 输入：读者合法账号密码
  - 预期：返回 `stateCode=2`；Session 写入 `readercard`
  - 断言点：读者分支命中；`findReaderCardByUserId` 被调用
  - 优先级：P0

- `WB-LOGIN-03`
  - 目标方法：`/api/loginCheck`
  - 覆盖类型：分支
  - 输入：非法账号或错误密码
  - 预期：返回 `stateCode=0`
  - 断言点：失败分支命中；Session 不应写入身份对象
  - 优先级：P0

- `WB-LOGIN-04`
  - 目标方法：`/api/loginCheck`
  - 覆盖类型：异常路径
  - 输入：`id=abc`
  - 预期：抛参数解析异常（当前实现）
  - 断言点：识别未处理异常路径，纳入缺陷分析
  - 优先级：P1

- `WB-LOGIN-05`
  - 目标方法：`/admin_repasswd_do`
  - 覆盖类型：分支
  - 输入：Session 无 `admin`
  - 预期：重定向登录页并提示会话失效
  - 断言点：`admin==null` 分支命中
  - 优先级：P0

### B. 图书维护（`BookController` + `BookService` + `BookDao`）

- `WB-BOOK-01`
  - 目标方法：`addBookDo`
  - 覆盖类型：分支 + 边界
  - 输入：`name` 长度 50（边界内）
  - 预期：校验通过，继续执行新增
  - 断言点：`validateBookTextLength` 返回 `null`
  - 优先级：P0

- `WB-BOOK-02`
  - 目标方法：`addBookDo`
  - 覆盖类型：分支 + 边界
  - 输入：`name` 长度 51（边界外）
  - 预期：校验失败并重定向新增页
  - 断言点：错误消息为“书名过长”
  - 优先级：P0

- `WB-BOOK-03`
  - 目标方法：`bookEditDo`
  - 覆盖类型：分支 + 边界
  - 输入：`isbn` 长度 14
  - 预期：校验失败并回编辑页
  - 断言点：错误消息为“ISBN 过长”
  - 优先级：P1

- `WB-BOOK-04`
  - 目标方法：`queryBookDo`
  - 覆盖类型：分支
  - 输入：可命中关键字
  - 预期：返回包含图书列表的 `admin_books`
  - 断言点：`exist=true` 分支命中
  - 优先级：P1

- `WB-BOOK-05`
  - 目标方法：`queryBookDo`
  - 覆盖类型：分支
  - 输入：不可命中关键字
  - 预期：返回错误提示“没有匹配的图书”
  - 断言点：`exist=false` 分支命中
  - 优先级：P1

- `WB-BOOK-06`
  - 目标方法：`BookDao.getBook`
  - 覆盖类型：数据流
  - 输入：存在的 `bookId`
  - 预期：`bookId` 定义后，所有字段正确赋值并被上层使用
  - 断言点：DU 链 `bookId -> query -> resultSet -> Book`
  - 优先级：P1

### C. 借还书流程（`LendController` + `LendService` + `LendDao`）

- `WB-LEND-01`
  - 目标方法：`bookLendDo`
  - 覆盖类型：分支 + 基本路径
  - 输入：合法 `bookId` 与 `readerId`，DAO 两次更新成功
  - 预期：借阅成功，跳转图书列表
  - 断言点：`lendsucc=true` 路径命中
  - 优先级：P0

- `WB-LEND-02`
  - 目标方法：`bookLendDo`
  - 覆盖类型：分支 + 缺陷验证
  - 输入：模拟 `lendService.bookLend=false`
  - 预期：业务应失败提示；当前代码仍提示成功（缺陷）
  - 断言点：失败路径命中但文案错误，记录为缺陷
  - 优先级：P0

- `WB-LEND-03`
  - 目标方法：`bookReturn`
  - 覆盖类型：分支
  - 输入：归还成功
  - 预期：提示“图书归还成功”
  - 断言点：`retSucc=true` 分支命中
  - 优先级：P0

- `WB-LEND-04`
  - 目标方法：`bookReturn`
  - 覆盖类型：分支
  - 输入：模拟归还失败
  - 预期：提示“图书归还失败”
  - 断言点：`retSucc=false` 分支命中
  - 优先级：P0

- `WB-LEND-05`
  - 目标方法：`LendService.bookLend`
  - 覆盖类型：基本路径
  - 输入：`bookLendOne>0` 且 `bookLendTwo>0`
  - 预期：返回 `true`
  - 断言点：双更新均成功路径
  - 优先级：P0

- `WB-LEND-06`
  - 目标方法：`LendService.bookLend`
  - 覆盖类型：基本路径
  - 输入：任一更新返回 0
  - 预期：返回 `false`
  - 断言点：逻辑与路径正确短路
  - 优先级：P0

- `WB-LEND-07`
  - 目标方法：`myLend`
  - 覆盖类型：分支 + 数据流
  - 输入：Session 无 `readercard`
  - 预期：重定向登录页
  - 断言点：`readerCard` 变量判空路径
  - 优先级：P0

### D. 读者信息与密码（`ReaderController`）

- `WB-READER-01`
  - 目标方法：`readerRePasswdDo`
  - 覆盖类型：分支
  - 输入：Session 无 `readercard`
  - 预期：跳转登录页并提示失效
  - 断言点：空会话分支命中
  - 优先级：P0

- `WB-READER-02`
  - 目标方法：`readerRePasswdDo`
  - 覆盖类型：分支
  - 输入：`newPasswd != reNewPasswd`
  - 预期：提示“两次输入不相同”
  - 断言点：新密码一致性分支
  - 优先级：P0

- `WB-READER-03`
  - 目标方法：`readerRePasswdDo`
  - 覆盖类型：分支
  - 输入：旧密码错误
  - 预期：提示“原密码错误”
  - 断言点：`passwd.equals(oldPasswd)` 的 false 路径
  - 优先级：P0

- `WB-READER-04`
  - 目标方法：`readerRePasswdDo`
  - 覆盖类型：基本路径 + 数据流
  - 输入：旧密码正确、新密码一致、更新成功
  - 预期：更新成功并刷新 Session 中 `readercard`
  - 断言点：DU 链 `readerCard(passwd)->比对->update->session刷新`
  - 优先级：P0

- `WB-READER-05`
  - 目标方法：`reader_edit_do_r`
  - 覆盖类型：分支 + 数据流
  - 输入：姓名变化
  - 预期：同时调用 `updateName` 与 `editReaderInfo`
  - 断言点：姓名变更分支，双写一致
  - 优先级：P1

- `WB-READER-06`
  - 目标方法：`reader_edit_do_r`
  - 覆盖类型：分支 + 数据流
  - 输入：姓名不变
  - 预期：只更新 `reader_info`
  - 断言点：姓名不变分支，避免冗余更新
  - 优先级：P1

- `WB-READER-07`
  - 目标方法：`reader_edit_do_r`
  - 覆盖类型：异常路径
  - 输入：`birth` 为非法日期字符串
  - 预期：当前实现捕获异常并继续，默认日期参与更新
  - 断言点：识别潜在数据污染风险并记录缺陷
  - 优先级：P1

## 3. 基本路径覆盖建议（最小集合）

为了控制工作量并满足作业要求，建议至少执行以下独立路径集合：

- `P-LOGIN`：管理员成功 / 读者成功 / 登录失败 / id 非法
- `P-BOOK`：新增通过 / 新增长度失败 / 编辑通过 / 编辑长度失败 / 查询命中 / 查询未命中
- `P-LEND`：借书成功 / 借书失败 / 还书成功 / 还书失败 / 我的借阅未登录
- `P-READER`：改密成功 / 新密码不一致 / 旧密码错误 / 会话失效 / 个人信息姓名变更 / 姓名不变

## 4. 数据流分析关注点（DU 链）

- `readerCard`：Session 读取 -> 判空 -> 取 `readerId`/`passwd` -> 调用 Service -> 更新 Session
- `bookId`：Request 参数 -> `parseInt` -> Service -> Dao SQL 参数 -> DB 状态变化
- `readerId`：Request/Session -> Service -> Dao where 条件 -> 借阅记录/读者信息
- `birth`：字符串定义 -> `parse` -> `Date` 使用 -> `reader_info.birth` 持久化
- `searchWord`：输入定义 -> `LIKE` 条件构造 -> 匹配计数/查询结果使用

## 5. 执行与统计建议

- 测试分层：Controller（MockMVC）+ Service（Mock Dao）+ Dao（连接测试库）
- 覆盖率统计：JaCoCo 输出类覆盖率、方法覆盖率、分支覆盖率
- 缺陷记录：对“借书失败提示仍成功”“非法参数导致 500”等问题给出复现步骤与证据截图
- 报告映射：每条测试用例关联需求编号（如 `FR-LOGIN-02`、`FR-LEND-02`）形成需求追溯

---

如需扩展到黑盒联动，可在每条 `WB-*` 用例后追加对应的 `BB-*` 用例编号，形成一一映射关系，便于最终报告展示“黑盒-白盒一致性”。
