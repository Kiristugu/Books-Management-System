# 基于大模型的软件测试使用记录（白盒测试）

> 说明：本记录用于课程作业提交，聚焦白盒测试过程。  
> 记录形式为“提示词（Prompt）+ 反馈摘要（Feedback）”，反馈按要求使用省略版表达。  
> 被测项目：`Books-Management-System`（Spring MVC + JDBC + MySQL/H2）

---

## 1. 任务启动与范围界定

### Prompt 1：白盒测试总体规划

请基于以下约束制定白盒测试方案：  
1) 覆盖率目标至少 95%；  
2) 测试方法必须包含代码覆盖、基本路径覆盖、数据流分析；  
3) 项目是 Java Web（Controller/Service/Dao 三层）；  
4) 输出应包含可执行顺序、优先级、预期交付件。

### Feedback 1（省略版）

- 建议优先测核心业务主链路：登录、图书维护、借还书、读者信息与密码……  
- 建议先做 Controller+Service 分支，再补 DAO 真实 SQL 路径……  
- 建议构建“白盒测试矩阵（方法-路径-覆盖类型-断言点）”作为主文档……

---

## 2. 需求追溯与白盒对象识别

### Prompt 2：从 SRS 反推白盒关注点

我将提供《需求说明文档》和项目代码，请将功能需求映射到白盒可测对象。  
请输出：  
1) 对应 Controller 方法；  
2) 关键分支；  
3) 关键变量数据流（DU 链）；  
4) 每项建议的测试优先级。

### Feedback 2（省略版）

- 识别出重点方法：`loginCheck`、`reAdminPasswdDo`、`bookEditDo`、`bookLendDo`、`readerRePasswdDo`、`reader_edit_do_r`……  
- 给出高优先级路径：会话失效、参数非法、成功/失败分支、校验边界分支……  
- 给出关键数据流：`bookId`、`readerId`、`readerCard`、`birth`、`searchWord`……

---

## 3. 白盒测试矩阵生成

### Prompt 3：生成可执行白盒测试矩阵

请按“模块 -> 用例编号 -> 覆盖类型 -> 输入 -> 预期 -> 断言点 -> 优先级”生成白盒测试矩阵。  
模块包含：  
A 登录与会话  
B 图书维护  
C 借还书流程  
D 读者信息与密码  
要求矩阵可直接转为测试代码实现。

### Feedback 3（省略版）

- 生成了 A/B/C/D 四模块矩阵，含 WB-LOGIN/WB-BOOK/WB-LEND/WB-READER 编号……  
- 每个用例附带分支命中点与失败路径说明……  
- 同时标注了数据流分析关注变量与基本路径最小集合……

---

## 4. A 模块（登录与会话）测试代码生成

### Prompt 4：按矩阵实现 A 模块单测

请为 `LoginController` 生成 TestNG 白盒测试代码：  
1) 管理员登录成功；  
2) 读者登录成功；  
3) 登录失败；  
4) 非法 id 异常路径；  
5) 管理员改密会话失效路径。  
要求可直接运行，必要时使用 Stub Service。

### Feedback 4（省略版）

- 已生成 `LoginControllerWhiteBoxTest`……  
- 处理了并行执行导致的测试干扰（设置 `singleThreaded`）……  
- A 模块测试通过……

---

## 5. B 模块（图书维护）测试代码生成

### Prompt 5：按边界与分支补 B 模块

请实现 `BookController` 白盒测试，至少包含：  
1) `name=50` 边界通过；  
2) `name=51` 边界失败；  
3) `isbn=14` 边界失败；  
4) 查询命中分支；  
5) 查询未命中分支。  
并补充必要的数据流断言。

### Feedback 5（省略版）

- 已生成 `BookControllerWhiteBoxTest`……  
- 修正了 `BookAddCommand.setPubdate` 参数类型适配问题……  
- B 模块测试通过……

---

## 6. C 模块（借还书）测试代码生成

### Prompt 6：覆盖 Controller + Service 双层路径

请实现 C 模块白盒测试：  
1) `LendController.bookLendDo` 成功/失败；  
2) `bookReturn` 成功/失败；  
3) `myLend` 会话空/非空；  
4) `LendService.bookLend/bookReturn` 的双更新真/假路径。  
对已知缺陷分支需保留现状断言并记录。

### Feedback 6（省略版）

- 已生成 `LendControllerWhiteBoxTest` 与 `LendServiceWhiteBoxTest`……  
- 覆盖了“借书失败仍提示成功”的缺陷分支……  
- C 模块测试通过……

---

## 7. D 模块（读者信息与密码）测试代码生成

### Prompt 7：覆盖读者控制器复杂分支

请实现 `ReaderController` 白盒测试：  
1) 改密会话失效/新密码不一致/旧密码错误/成功；  
2) `reader_edit_do_r` 姓名变化与不变化路径；  
3) 成功路径需要断言 Session 刷新。  
要求断言消息、跳转、调用路径一致。

### Feedback 7（省略版）

- 已生成 `ReaderControllerWhiteBoxTest`……  
- 关键分支、重定向和会话刷新已覆盖……  
- D 模块测试通过……

---

## 8. 覆盖率驱动迭代（JaCoCo）

### Prompt 8：接入覆盖率工具并评估缺口

请将 JaCoCo 集成到 Maven，执行当前测试并输出总体覆盖率（Instruction/Branch/Line/Method），  
然后根据缺口提出下一轮补测优先级。

### Feedback 8（省略版）

- 已在 `pom.xml` 接入 `jacoco-maven-plugin`……  
- 初始覆盖率偏低，识别到未覆盖集中于 web/dao/domain……  
- 建议先补 service 全路径，再补 dao 集成测试……

---

## 9. DAO 白盒集成测试（H2）

### Prompt 9：构建 DAO 集成测试基座

请使用 H2 内存库为 DAO 层建立可重复的白盒集成测试：  
1) 建表与种子数据初始化；  
2) 覆盖 `AdminDao/BookDao/LendDao/ReaderCardDao/ReaderInfoDao` 的主要 SQL 路径；  
3) 验证 `RowCallbackHandler` 数据映射。  
并保持测试可并行稳定执行。

### Feedback 9（省略版）

- 已新增 H2 测试依赖与 DAO 测试基座……  
- 已完成五个 DAO 集成测试类……  
- DAO 路径覆盖显著提升……

---

## 10. 分支缺口定点补测

### Prompt 10：按分支缺口进行精准补测

请根据 JaCoCo 未覆盖分支清单，做最小增量补测：  
1) LoginController 页面/路由分支；  
2) BookController 文本长度剩余边界分支；  
3) ReaderController 日期解析异常 catch 分支；  
4) LendService 短路分支。  
最终目标：Line 和 Branch 均达到 95%。

### Feedback 10（省略版）

- 已完成分支缺口补测并全量回归通过……  
- Line 覆盖率超过 95%，Branch 覆盖率超过 95%……  
- 白盒覆盖目标满足作业要求……

---

## 11. 执行命令记录（文字版）

以下为关键执行命令（摘要）：

```text
mvn -Dtest=LoginControllerWhiteBoxTest test
mvn -Dtest=BookControllerWhiteBoxTest test
mvn "-Dtest=LendControllerWhiteBoxTest,LendServiceWhiteBoxTest" test
mvn -Dtest=ReaderControllerWhiteBoxTest test
mvn clean test
```

覆盖率报告产物（摘要）：

```text
target/site/jacoco/index.html
target/site/jacoco/jacoco.csv
target/jacoco.exec
```

---

## 12. 阶段性结果总结（白盒方法视角）

- 代码覆盖法：通过 JaCoCo 持续发现缺口并迭代补测，覆盖率达到目标阈值……  
- 基本路径覆盖法：对关键控制器与服务方法完成成功/失败/异常/会话失效路径覆盖……  
- 数据流分析法：验证 `bookId/readerId/readerCard/birth` 等关键变量定义-使用链……  
- DAO 白盒集成：在 H2 上执行真实 SQL 和映射逻辑，提升实现层覆盖深度……

---

## 13. 可选附录（提交时可配套截图）

建议截图清单（本文件仅文字记录）：

1. 提示词输入截图（10 组核心 Prompt）  
2. 关键代码生成结果截图（A/B/C/D 与 DAO）  
3. JaCoCo 报告首页与包级覆盖率截图  
4. 全量测试通过日志截图（`Tests run / Failures / Errors`）

> 备注：如需简化版提交，可保留第 1、3、4、10、12 节作为最小证据链。
